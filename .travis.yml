language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t marlanfer/docker-react -f Dokerfile.dev .
script:
  - docker run -e CI=true marlanfer/docker-react npm run test